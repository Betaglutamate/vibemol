"""VibeMol — a web-native, open-source molecular viewer.

The backend parses molecular structures, evaluates a PyMOL-style atom-selection
language, computes geometry, and streams it to a WebGL frontend. It owns the
*scene*; the browser owns the *camera*.
"""

__version__ = "0.0.1"
