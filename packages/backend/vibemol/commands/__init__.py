"""The command registry and PyMOL-compatible command implementations.

Phase 1 adds a registry plus ``load, fetch, show, hide, as, color, select,
set, bg_color, zoom, orient, turn, remove, delete`` — the surface the web
command console maps text input onto.
"""
