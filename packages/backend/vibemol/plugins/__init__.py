"""The plugin system.

Phase 3 adds entry-point-based discovery (``importlib.metadata``) and a stable,
versioned ``PluginContext`` so third-party packages can register new commands,
representations, file parsers, and analyses.
"""
